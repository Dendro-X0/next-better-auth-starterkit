# ------------------------------------------------------------------
#  APP
# ------------------------------------------------------------------
# The URL of your app, required for authentication.
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ------------------------------------------------------------------
#  DATABASE
# ------------------------------------------------------------------
# Replace with your actual PostgreSQL connection string.
DATABASE_URL="postgresql://user:password@host:port/db"

# ------------------------------------------------------------------
#  AUTHENTICATION (Better Auth)
# ------------------------------------------------------------------
# A secret key for signing authentication tokens.
# You can generate a new one by running: openssl rand -hex 32
BETTER_AUTH_SECRET="your-super-secret-auth-secret-generated-with-openssl"

# ------------------------------------------------------------------
# OAUTH (Google & GitHub)
# ------------------------------------------------------------------
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"
GITHUB_CLIENT_ID="your-github-client-id"
GITHUB_CLIENT_SECRET="your-github-client-secret"

# ------------------------------------------------------------------
#  EMAIL (Resend / SMTP)
# ------------------------------------------------------------------
# Choose which provider to use for sending emails: RESEND or SMTP
MAIL_PROVIDER="RESEND"

# If using RESEND, set your API key. Optional when using SMTP only.
RESEND_API_KEY="your-resend-api-key"

# The "from" email address for sending emails.
# Optional in local development; will default to "onboarding@resend.dev" if omitted.
EMAIL_FROM="noreply@example.com"

# SMTP settings (use with tools like MailHog on local dev)
# When MAIL_PROVIDER=SMTP, these are used to send emails.
SMTP_HOST="localhost"
SMTP_PORT="1025"
SMTP_SECURE="false" # true for 465, false for others
# Optional auth; leave blank for MailHog
SMTP_USER=""
SMTP_PASS=""

# ------------------------------------------------------------------
#  RATE LIMITING (Upstash Redis) — Optional
# ------------------------------------------------------------------
# Configure these to enable distributed rate limiting for auth actions
# (signup, login, forgot password, magic link, 2FA verify, reset password).
# If not set (e.g., in local development), rate limiting becomes a safe no-op.
UPSTASH_REDIS_URL="https://your-upstash-redis-url"
UPSTASH_REDIS_TOKEN="your-upstash-redis-rest-token"

# ------------------------------------------------------------------
#  SMS (optional, for SMS MFA)
# ------------------------------------------------------------------
SMS_PROVIDER="CONSOLE" # or TWILIO
TWILIO_ACCOUNT_SID="your-twilio-account-sid"
TWILIO_AUTH_TOKEN="your-twilio-auth-token"
TWILIO_FROM_NUMBER="+10000000000"

# ------------------------------------------------------------------
#  LOCAL DEVELOPMENT AUTH GUARD (optional)
# ------------------------------------------------------------------
# Default is strict. Setting this to off disables middleware redirects and server-side guards in non-production.
AUTH_GUARD_MODE=off

# Dev user identity used when AUTH_GUARD_MODE=off
AUTH_DEV_USER_ID="dev-user"
AUTH_DEV_USER_EMAIL="dev@example.com"
AUTH_DEV_USER_NAME="Dev User"